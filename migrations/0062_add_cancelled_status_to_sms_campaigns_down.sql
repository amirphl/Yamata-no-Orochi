-- Migration: 0062_add_cancelled_status_to_sms_campaigns_down.sql
-- Description: Down migration for adding 'cancelled' to sms_campaign_status enum (no-op)

-- DOWN MIGRATION (no-op)
-- Removing enum values safely requires recreating the type; skipping for rollback safety.
