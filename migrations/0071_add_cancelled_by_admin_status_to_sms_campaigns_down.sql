-- Migration: 0071_add_cancelled_by_admin_status_to_sms_campaigns_down.sql
-- Description: Down migration for adding 'cancelled-by-admin' to sms_campaign_status enum (no-op)

-- DOWN MIGRATION (no-op)
-- Removing enum values safely requires recreating the type; skipping for rollback safety.
